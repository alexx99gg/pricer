spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    password: sa
    username: sa
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
logging:
  level:
    org.springframework: DEBUG
    com.sillycom.pricer: DEBUG